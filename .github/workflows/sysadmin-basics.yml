name: My First Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run commands
        run: |
          echo "Hello from GitHub Actions"
          date
          uname -a
